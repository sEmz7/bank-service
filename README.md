# 💳 Bank Cards Service  
REST-сервис для управления банковскими картами пользователей

## 📌 Описание проекта

Bank Cards Service — это backend-приложение на **Spring Boot**, предоставляющее функционал:
- создания и управления банковскими картами;
- авторизации и обновления JWT-токенов;
- выполнения переводов между собственными картами пользователя;
- административного управления пользователями и картами.

Проект включает:
- строгую ролевую модель (`USER`, `ADMIN`);
- JWT-аутентификацию и refresh-механику;
- пагинацию, фильтрацию, обработку ошибок;
- валидацию входных данных;
- подробную API-документацию (Swagger / OpenAPI).

---

## 🚀 Технологии

- **Java 21**
- **Spring Boot 3**
  - Spring Web
  - Spring Validation
  - Spring Security (JWT)
  - Spring Data JPA (Hibernate)
- **PostgreSQL**
- **Liquibase**
- **MapStruct**
- **Lombok**
- **Swagger**
- **JUnit + MockMvc** (интеграционные тесты)

## 🔐 Аутентификация и авторизация

Используется JWT-токен:

- `POST /auth/login` → выдаёт `access_token + refresh_token`
- `POST /auth/refresh` → обновляет токен `access_token`
- Доступ к пользовательским и админским эндпоинтам защищён `JwtFilter`

Пример заголовка:

```
Authorization: Bearer <token>
```

JWT включает:
- `username`
- `role`
- срок действия

**Роли**:
- `USER` может управлять только своими картами
- `ADMIN` имеет полный доступ к пользователям и картам

## 🗃 Сущности

### `User`
- `id`
- `username` (уникальный)
- `password` (BCrypt)
- `role` (`USER` / `ADMIN`)

### `Card`
- `id`
- `owner` (User)
- `cardNumber`
- `last4`
- `expiryDate`
- `status` (`PENDING`, `ACTIVE`, `BLOCK_PENDING`, ...)
- `balance`

---

## Инструкция по запуску:

### 🔧 Требования

Перед запуском убедитесь, что установлены:

- **Docker**
- **Docker Compose**

Java и Maven НЕ требуются — всё работает в контейнере.

---

## ▶️ Запуск приложения

### 1️⃣ Создать `.env`

Создайте файл `.env` в корне проекта:

```
POSTGRES_DB=cardsdb  
POSTGRES_USER=user  
POSTGRES_PASSWORD=pass  
POSTGRES_URL=jdbc:postgresql://localhost:5432/cardsdb  
JWT_SECRET=rOmVtoPS7FaHfgf8vhswtsqbiLtWT5QUJOv9OqwTex87hJ7FEVBGNAfgSWj0Lzi2
```

### 2️⃣ Сборка и запуск

В корневой директории:

```bash
docker compose up -d --build
```
---
## 🧩 Спецификация

После запуска:
### Swagger UI:

```
http://localhost:8080/swagger-ui/index.html
```

### OpenAPI YAML:

```
http://localhost:8080/v3/api-docs.yaml
```

---

## 🛑 Остановка приложения

```bash
docker compose down
```

С удалением volume:

```bash
docker compose down -v
```
---
## Контакты:

Telegram: https://t.me/s3mmm_7

Почта: semyondiulin@yandex.ru
